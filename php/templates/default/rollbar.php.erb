<?php

    require_once '<%= @roolbar_lib %>';

    $config = array(
        'access_token' => '<%= @rollbar_token %>',
        'environment' => '<%= @env %>',
        'root' => '<%= @current_dir %>',
        'branch' => '<%= @rollbar_branch %>',
        'included_errno' => <%= @rollbar_level %>
    );

    Rollbar::init($config);

?>


